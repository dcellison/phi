# Part 8: Answers and reasoning

1. Use the registered compound because the Phi analysis is already stable and useful.
2. Use a productive name-form if one two-, three-, or four-syllable token serves and the bearer accepts it. Use a guest frame for a five-or-more-syllable, multi-token, or otherwise non-onym adaptation; retain an exact record if spelling also matters.
3. Use exact form because every character identifies the object.
4. Use exact form because the inherited wording is itself under analysis.
5. Investigate core composition or a core proposal; recurrence in original discourse is evidence of a genuine semantic gap, though it does not by itself decide that a new root is needed.
6. Use exact form because changing the precision could cause harm.
7. Lowercase and revalidate the guest adaptation, or use exact form if the case must remain.
8. Add `hasho`; every occurrence is fully bounded.
9. Encode the literal occurrence as doubled `patho patho`, then use a later single `patho` to close.
10. Move `ne` before `patha`; Phi grammar scopes over the whole atom from outside.
11. Restore `hasha` and `hasho`. Introduction creates familiarity, not lexicon membership.
12. Add an ordinary Phi support verb after the frame.

13.

```
mia patha F = ma patho theo.
1SG EXT.EXACT [F = ma] EXT.EXACT.CLOSE read.
(I read "F = ma.")
```

14. One possible adapted form is `hamina`:

```
ne patha Amina patho so shua.
NAME EXT.EXACT [Amina] EXT.EXACT.CLOSE FUT come.
(Amina will come.)

ne hasha hamina hasho so shua.
NAME EXT.GUEST [hamina] EXT.GUEST.CLOSE FUT come.
(Amina will come.)

ne hamina so shua.
NAME hamina FUT come.
(hamina will come.)
```

The last form is not an elided guest frame. It is a separate productive onym licensed directly by `ne`, and it is appropriate only if the bearer accepts `hamina` as their Phi-form name.

15.

```
shia patha master patho to haolu.
3SG EXT.EXACT [master] EXT.EXACT.CLOSE PST speak.
(They said the word "master.")
```

16.

```
mia wei patha https://example.net/a?x=1 patho phelui sepho.
1SG DAT EXT.EXACT [https://example.net/a?x=1] EXT.EXACT.CLOSE word send.
(I send a word to https://example.net/a?x=1.)
```

17. The doubled pair is payload; the final single occurrence closes:

```
mia patha source patho patho text patho theo.
1SG EXT.EXACT [source patho patho text] EXT.EXACT.CLOSE read.
(I read source text containing the literal token "patho.")
```
